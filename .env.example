# ==============================================================================
# Grow Ministry Website — Environment Variables
# ==============================================================================
# Copy this file to .env.local and fill in the values:
#   cp .env.example .env.local
#
# IMPORTANT: Never commit .env.local to version control.
# ==============================================================================

# ------------------------------------------------------------------------------
# GoHighLevel CRM Integration
# ------------------------------------------------------------------------------
# Server-side webhook URL for API routes (contact form, lead capture, cookie consent).
# This is NOT exposed to the browser — all client forms route through /api/lead.
# Get this from your GoHighLevel account: Automation → Webhooks → Create Webhook
GHL_WEBHOOK_URL=https://services.leadconnectorhq.com/hooks/YOUR_WEBHOOK_ID

# ------------------------------------------------------------------------------
# Mapbox (Travel Page Map)
# ------------------------------------------------------------------------------
# Public Mapbox GL access token for the interactive travel map.
# Get this from your Mapbox account: https://account.mapbox.com/access-tokens/
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=pk.YOUR_MAPBOX_TOKEN

# ------------------------------------------------------------------------------
# reCAPTCHA v3 (Contact Form)
# ------------------------------------------------------------------------------
# Public site key (loaded client-side).
# Get from https://www.google.com/recaptcha/admin
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=6Lxxxxx

# Server-side secret key for reCAPTCHA verification.
# This is NOT exposed to the browser.
RECAPTCHA_SECRET_KEY=6Lxxxxx

# ------------------------------------------------------------------------------
# GoHighLevel API (Private Integration)
# ------------------------------------------------------------------------------
# Private Integration Token for creating contacts via the GHL API.
# Get from GoHighLevel: Settings → Integrations → Private Integrations
GHL_API_TOKEN=pit-xxxxxxxxxxxx

# Location (Sub-Account) ID for the GHL CRM.
# Get from GoHighLevel: Settings → Business Info → Location ID
GHL_LOCATION_ID=xxxxxxxxxxxxxxxxxxxx

# ------------------------------------------------------------------------------
# Supabase (Database + Auth)
# ------------------------------------------------------------------------------
# Public URL and anon key (safe for browser — protected by RLS).
# Get from Supabase Dashboard: Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGci...

# Service role key (server-side only — bypasses RLS for API inserts).
# Get from Supabase Dashboard: Settings → API → service_role
SUPABASE_SERVICE_ROLE_KEY=eyJhbGci...

# ------------------------------------------------------------------------------
# Google Analytics 4
# ------------------------------------------------------------------------------
# GA4 measurement ID for analytics tracking.
# Get from Google Analytics: Admin → Data Streams → Web
NEXT_PUBLIC_GA4_MEASUREMENT_ID=G-XXXXXXXXXX

# ------------------------------------------------------------------------------
# Sentry Error Monitoring
# ------------------------------------------------------------------------------
# Sentry DSN for error tracking (public — client-side).
# Get from Sentry: Settings → Client Keys (DSN)
NEXT_PUBLIC_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# Auth token for source map upload (CI only, not needed locally).
# Get from Sentry: Settings → Auth Tokens
SENTRY_AUTH_TOKEN=sntrys_xxx

# ------------------------------------------------------------------------------
# Upstash Redis (Rate Limiting)
# ------------------------------------------------------------------------------
# Redis REST API for rate limiting.
# Get from Upstash Console: https://console.upstash.com
UPSTASH_REDIS_REST_URL=https://xxx.upstash.io
UPSTASH_REDIS_REST_TOKEN=AXxx
